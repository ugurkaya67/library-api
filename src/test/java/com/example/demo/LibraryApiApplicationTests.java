package com.example.demo;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest // Vérifie que tout le contexte de l'application se charge correctement
class LibraryApiApplicationTests {

    @Test
    void contextLoads() {
        // Test simple pour vérifier que l'application démarre sans erreur
    }
}
